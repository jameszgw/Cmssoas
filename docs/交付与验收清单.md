# CMSSOAS 交付与验收清单

> 版本：v1.0 · 日期：2026-06-07 · 初始账号：`admin / 8888`
> 配套：[自测报告（中）](自测报告.md) · [Self-Test Report (EN)](self-test-report-en.md)

本清单用于交付方与验收方逐项核对。验收方式分三类：**A 自动化测试**、**B 接口/命令实跑**、**C 界面核对**。验收结论：所有项已自测通过（见自测报告），勾选即代表复核确认。

---

## 一、交付物清单

| 类别 | 交付物 | 路径 |
|---|---|---|
| 方案文档 | 00 主文档 + 01–07 专题 | `docs/00..07` |
| 运营后端 | Spring Boot 服务（含 Flyway 迁移、种子） | `server/license-platform` |
| 运营前端 | Vue3 + TS 控制台 | `web/console` |
| 客户端 SDK | Ed25519 / SM2 验签 + 门禁 + demo | `sdk/license-sdk` |
| 代码保护示例 | 类加密+License 绑定 + 混淆 profile | `examples/protected-app` |
| 部署/可观测 | Docker Compose + Prometheus/Grafana | `docker-compose.yml`、`infra/` |
| CI/CD | 8 条流水线 + 制品/覆盖率/Release | `.github/workflows/ci.yml` |
| 测试与报告 | 单测/集成/E2E + 自测报告（中英） | `*/src/test`、`web/console/e2e`、`docs/` |

---

## 二、功能验收项（对应自测 P0/P1/P2）

### P0 核心主链路
- [x] 登录鉴权：admin/8888 发 JWT；错误密码/无 token→401 — *A/B*
- [x] 接口级权限：越权操作→403，授权操作→200 — *A/B*
- [x] License 签发：服务端 Ed25519 签名，生成可下载 .lic — *B/C*
- [x] License 吊销：状态 REVOKED + 进 CRL + 心跳实时拒绝 — *B*
- [x] 并发席位：超并发激活→409 — *B*
- [x] 心跳防重放：重复 nonce→REPLAY — *B*
- [x] 订阅自动签发：订阅→自动签发对应 License（席位=套餐×数量） — *A(E2E)/B/C*
- [x] SDK 门禁：未授权/越版本/篡改→拒绝 — *A/B*

### P1 重要功能
- [x] 租户开通 + 邮件事务发件箱（异步+重试） — *B*
- [x] 一次性激活 + MFA（TOTP）绑定，token 一次性失效 — *B*
- [x] License 生命周期：续期/变更/吊销 + 版本号 + 历史 diff — *B/C*
- [x] 在线监控：在线/宽限/席位 KPI + 实例表 — *C*
- [x] 宽限期回收：超期心跳→定时回收席位 — *B*
- [x] RBAC：角色多态权限树（无/只读/编辑/完全 + 级联） — *A(E2E)/C*
- [x] 用户管理：建/重置/启停，admin 不可停 — *B/C*
- [x] 首登强制改密 — *B/C*
- [x] 产品-模块-功能-版本矩阵 + 套餐 SKU — *B/C*
- [x] 国密 SM2 验签：服务端签发 + SDK 验签 + CI 矩阵冒烟 — *A/B*
- [x] 可观测：Actuator 健康 + Prometheus 指标 — *B*

### P2 体验与边界
- [x] 换肤（6 主题）/ 2K-4K 自适应 / 中英 i18n — *C*
- [x] 业务数据等宽字体与帮助字体区分 + 操作帮助 — *C*
- [x] 代码保护：合法运行 / 换证解密失败 / 篡改验签失败 / 明文移除 — *B*
- [x] 混淆加固：`mvn -Pharden` 辅助类改名、入口保留 — *A(CI)/B*
- [x] 按钮级权限：受限角色隐藏敏感按钮 — *C*
- [x] 边界异常：缺参 400 / 未知资源 404 / 席位复用 / 吊销后激活 403 — *B*
- [x] Docker Compose 配置校验通过 — *B*

---

## 三、质量门禁（CI 必过）
- [x] `backend` 后端构建 + 测试（9）+ 覆盖率(JaCoCo)
- [x] `sdk` SDK 测试（8）+ 覆盖率
- [x] `sign-smoke` Ed25519/SM2 端到端验签矩阵
- [x] `harden` ProGuard 混淆制品产出与校验
- [x] `frontend` 前端构建
- [x] `e2e` Playwright 端到端（6）
- [x] `ci-summary` 结果汇总 / `release` 打 tag 聚合制品发布

> 当前实测：后端 9/9、SDK 8/8、E2E 6/6 全绿；签名双算法冒烟通过。

---

## 四、验收快速复跑
```bash
# 一键起（需 Docker 守护进程）
docker compose up -d --build           # 前端 http://localhost  后端 :8080  Grafana :3000

# 或本地开发态
cd server/license-platform && mvn spring-boot:run     # admin/8888
cd web/console && npm install && npm run dev

# 测试
cd server/license-platform && mvn test                # 9/9
cd sdk/license-sdk && mvn test                        # 8/8
cd web/console && npx playwright test                 # 6/6（后端需在 :8080）
```

---

## 五、已知约束与后续建议
- 演示态私钥落盘（`var/keys`），生产须入 KMS/HSM；邮件默认 `log` 落盘，配置 SMTP 后真实外发。
- 纯软件保护不能绝对防破解，目标是抬高成本 + 在线吊销/水印溯源 + 法律协议综合防线。
- 建议后续：私钥托管 KMS、后端多实例 + 负载均衡、数据库主从备份、网关限流 + mTLS、日志/链路追踪接入。

---

## 六、发版操作说明（Release）

CI 已配置：推送 `v*` 标签触发 `release` 流水线 → 下载各任务制品（覆盖率 / E2E 报告 / 混淆制品）→ 打包 `cmssoas-artifacts.zip` + 生成发布说明 → 创建 GitHub Release 并附件。

**发版步骤（任选其一）：**
1. 命令行打 tag 并推送（具备 tag 推送权限时）：
   ```bash
   git checkout main && git pull
   git tag -a v1.0.0 -m "CMSSOAS v1.0.0"
   git push origin v1.0.0          # 触发 release 流水线
   ```
2. GitHub 网页：Releases → Draft a new release → 选择/新建 tag `v1.0.0` → Publish（同样触发流水线）。

**版本号约定**：遵循 SemVer（`vMAJOR.MINOR.PATCH`）。首个交付版本建议 `v1.0.0`。
**前置条件**：`main` 分支 CI 全绿（backend/sdk/sign-smoke/harden/frontend/e2e 均通过）。
**回滚**：删除错误 tag `git push origin :refs/tags/vX.Y.Z` 并在 GitHub 删除对应 Release。

> 注：受限的托管环境仅允许推送指定特性分支，`refs/tags/*` 推送会被拒（HTTP 403）；
> 发版需在具备标签推送权限的环境，或经合并入 `main` 后由仓库侧执行。

## 七、签收
| 角色 | 姓名 | 日期 | 结论 |
|---|---|---|---|
| 交付方 |  |  | ☐ 通过 ☐ 驳回 |
| 验收方 |  |  | ☐ 通过 ☐ 驳回 |
