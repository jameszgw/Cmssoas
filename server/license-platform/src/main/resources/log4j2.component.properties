# 强制 log4j-api 使用 log4j-core 工厂(供在线加固的 ProGuard 使用),
# 避免与 Spring Boot 的 log4j-to-slf4j 桥接同时存在时产生 LoggerContext ClassCastException。
# 应用自身日志仍走 SLF4J/Logback(应用代码直接用 slf4j,不受影响)。
log4j2.loggerContextFactory=org.apache.logging.log4j.core.impl.Log4jContextFactory
