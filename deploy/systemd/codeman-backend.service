# CODEMAN 后端 systemd 单元（CentOS7）
# 安装：cp 到 /etc/systemd/system/ ；jar 放 /opt/codeman/backend/app.jar ；环境变量见 /etc/codeman/backend.env
[Unit]
Description=CODEMAN License Platform Backend
After=network.target

[Service]
Type=simple
User=codeman
Group=codeman
WorkingDirectory=/opt/codeman/backend
EnvironmentFile=/etc/codeman/backend.env
# JDK21（Temurin）建议装到 /opt/jdk-21，并在此指定绝对路径
ExecStart=/opt/jdk-21/bin/java -Xms512m -Xmx1g -Dstdout.encoding=UTF-8 -jar /opt/codeman/backend/app.jar
SuccessExitStatus=143
Restart=on-failure
RestartSec=5
# 简单加固
NoNewPrivileges=true
PrivateTmp=true

[Install]
WantedBy=multi-user.target
