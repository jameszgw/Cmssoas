# Prometheus 抓取配置：定时拉取后端 Actuator 暴露的指标
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: cmssoas-backend
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ['backend:8080']
